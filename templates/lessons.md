# Lessons & Gotchas — <project>

One entry per gotcha. Format: **date · context · the lesson · durability guess**.

Promotion: when a lesson recurs outside this project (threshold: two
independent occurrences), take it through your durable-wiki ingest flow — don't
copy entries into the wiki directly, synthesize the general principle. See the
methodology this template ships with.

Keep the bar high: capture the things that cost real time and weren't obvious —
the ones where the types, the docs, or your assumption were wrong. Skip trivia.

Never capture secrets, credentials, tokens, internal hostnames, or customer
identifiers — sanitize stack traces and logs before pasting, and again before
promotion.

---

## YYYY-MM-DD — <one-line title of the lesson>

Context: <what you were doing when it bit you>.
- <the snag, and the workaround that actually fixed it>.
- <why the obvious approach failed — the non-obvious part>.
Durability: <project-specific | likely-general> — <one line of reasoning>.
